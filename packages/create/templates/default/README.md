# Fantoche project

Bootstrapped with `npm create @fantoche-dev`.

## Commands

- `npm start` — open the editor at http://localhost:9000
- `npm run render` — render the project to `output/video.mp4` headless

The example scene lives in `src/example.tsx`. Fonts are bundled locally
(`src/fonts/`) so rendering works offline.
